# 研究路线图：稳定地提高胜率与收益（2026-09-27 起；有进展就更新「状态」）

用户（2026-09-27）：「结合日 周 月线来找出上涨股票和卖顶的特征 根据现在的所有研究方向 因子搭配，错峰等等 制定一个可以稳定增长胜率和收益率的
研究方向进行继续研究 不限于现在的研究方向 可以开发出另外的研究方向」。
这份文件只写方向、理由、顺序与判定口径；每一项研究的规则写在各自脚本开头（先提交后运行），结果与决定记在 `var/sim_changes.md`。

## 0. 到现在学到的（约 30 项事先登记的研究）
1. **挑买点几乎都不成立**：47 个因子的 42,749 个组合 BH 校正后 0 个显著（`factor_combo_study`）；72 个因子的机器学习 IC 只有 0.02〜0.05、
   而且不稳定（`ml_study`）；买点质量分 AUC 0.50〜0.55（`score_study`）；动量类因子 2019〜2023 与 2023 以后方向相反。
2. **外部因子没有「错峰」**：利率、汇率、油价、商品、行业联动、上下游、主题、能源需求 → 当天 / 隔夜就反映，之后没有稳定的预测力
   （`lag_study`、`leadlag_study`、`supply_chain_study`、`theme_study`、`energy_study`、`lagscan_study`）。
3. **现行策略靠赔率赚钱**（个股胜率约 40〜46%）：提高胜率的过滤（真突破、跳过低分）会砍掉大赢家，组合更差（`signal_study`、`score_study`）。
4. **出场几乎都是日线 MACD 死叉**：止盈 / 跟踪止损 / 最长持有几乎不起作用；关掉死叉 → 交易少一半、胜率 43% → 48%、组合更差（`param_study`）；
   离场后 20 日价格比卖价低的只有 45%（`signal_study`）→ 卖点还有改进的余地，但「一直拿着」不是答案。
5. **两期都成立的只有少数**：量比最高 1/5、X2 顾客业种短観（已在前向记录里）；ml_study 里对 USD/JPY、美 10Y、WTI 敏感度高的票与
   予想利润收益率高的票两期都更好（但这是看了留出期才知道的 → 只能前向检验）。
6. **一个口径里通过的改动，换口径可能反过来**：一手放宽（yfinance 行情 +0.076 → J-Quants 行情 −0.035，2026-09-27 撤回）→ 每个候选都要多口径复现。
7. **样本**：日経225 20 年只有约 480〜650 笔突破（80% 检出力只能分辨 AUC ≥ 0.57）；时点 TOPIX 1000 10 年约 2,200 笔。

## 1. 「稳定」怎么量（以后每项研究都用）
- 主目标：组合 Calmar（年化 ÷ 最大回撤）；同时看个股胜率、每笔期望、每年的收益有几年比现行好。
- 三关都过才算通过：**V** 验证期（2019-01〜2023-09）比现行好；**H** 留出期（2023-10〜）与两个半段各自 Calmar ≥ 现行 + 0.05、回撤不更深；
  **R** 另一个股票池（今天的日経225 = 实盘股票池）不更差、胜率不低 2 pp 以上。
- 通过也只是提议：用户在对话里确认才改模拟盘；上了之后有前向记录（只追加，到期按事先的规则判定）。
- 一次一项；每项候选 ≤ 5〜6 个；挑选只用验证期，留出期只看一次。

## 2. 方向（按顺序）
| 编号 | 方向 | 为什么值得做 | 第一步 | 状态 |
|---|---|---|---|---|
| R1 | **多周期（日 / 周 / 月线）：上涨股票的特征与卖顶** | 周 / 月线只试过「趋势过滤」（exec_timing_study，647 笔，不显著）；卖出一侧没试过，而出场几乎都是日线死叉（0-4） | `scripts/mtf_study.py`：12 条周 / 月线买点规则、6 个见顶特征、条件出场（强势时改用周线出场）、周线出场替换、周线突破系统 | 2026-09-27 登记 |
| R2 | **第二条收益来源（和突破低相关）** | 只有一种买点 → 收益集中在少数大赢家、年份之间波动大；低相关的第二来源能让每年更稳 | 候选：上升趋势中的短期回调买入（日线超卖 + 周 / 月线向上，胜率高、持有短）；周线突破系统（R1 的 C5）；东证 TOPIX-17 业种 ETF 的月度动量轮动（替代部分 1655 的闲置资金）。先量和现行的日收益相关 | 未开始 |
| R3 | **需求面 / 资金流（新数据）** | 以前的外部因子都是价格类；投资主体的买卖是另一类信息，J-Quants Standard 就有 | 投資部門別売買状況（海外投资者 / 个人 / 信托银行的每周净买卖）、市场整体的信用残高、业种别空売り比率 → 先查覆盖与公布时点，再登记「新仓倍数」或 1655 择时的候选 | 未开始 |
| R4 | **事件与制度（新方向）** | 和因子无关、机制清楚的价格效应 | 时点 TOPIX / 日経225 成分的纳入与剔除（月末快照）、株式分割（AdjFactor 的生效日）、配当権利落ち与季节性（3 月 / 9 月、月末月初）→ 先做事件研究（只描述），稳定的再登记交易候选 | 未开始 |
| R5 | **前向验证（一直做）** | 看过留出期才挑出来的东西只能用以后的数据检验 | 已有：量比、X2、买点质量分、K4 能源、威胁指数各版本（季度复核）；可加：ml_study 两期同号的因子（敏感度、利润收益率、信用卖残）→ 要用户同意再登记 | 进行中 |
| R6 | **执行与成本** | 小但确定：滑点、寄付の気配、1655 拆单 | 立花上线后比较真实成交与回测；模拟期结束（2026-12-24）时量化 | 等立花 |

## 3. 下一步
- R1：登记 → 运行 → 结果写进 `var/sim_changes.md`，这里更新状态。
- 之后按 R2 → R3 → R4 一次一项；用户可以随时改顺序或加方向。新出现的行业 / 主题先用 `scripts/theme_link_check.py` 做关联对比（CLAUDE.md）。
